gate.script:
  executable: python
  subcommand: []
  modes:
    - check
    - apply
  capabilities:
    - Gates
  normalizer: generic_exit
  cli:
    paths:
      style: positional
  option_order:
    - paths
  scope:
    delivery: root
