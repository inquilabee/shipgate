jscpd.check.python:
  executable: jscpd
  subcommand: []
  modes:
    - check
  capabilities:
    - Quality
  normalizer: generic_exit
  install:
    manager: binary
    package: jscpd
    version: '>=3.0'
    binary: jscpd
  cli:
    paths:
      style: positional
    config:
      flag: --config
      style: scalar
    threshold:
      flag: --threshold
      style: scalar
      default: '2'
  configuration:
    bundled: configs/jscpd.python.json
    discover:
      - .shipgate/configs/jscpd.python.json
      - .jscpd.python.json
      - jscpd.python.json
    precedence:
      - cli
      - repo
      - bundled
    merge: false
  option_order:
    - config
    - threshold
    - paths
  scope:
    extensions:
      - .py
    delivery: root
