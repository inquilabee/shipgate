pytest.run:
  executable: pytest
  subcommand: []
  modes:
    - check
  capabilities:
    - Testing
  normalizer: pytest
  install:
    manager: python
    package: pytest
    version: '>=8.0'
  cli:
    paths:
      style: positional
  option_order:
    - paths
  scope:
    delivery: root
