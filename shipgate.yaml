suite: full
env: system
target: .
error-format: compact
configs:
  mode: auto
checks:
  semgrep.scan:
    scope: semgrep
  gate.module-size: {}
scopes:
  semgrep:
    target: .
    exclude:
      - src/shipgate/frontend/templates/
