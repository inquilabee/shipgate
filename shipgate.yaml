suite: full
env: system
target: .
error-format: compact
configs:
  mode: auto
checks:
  semgrep.scan:
    scope: semgrep
scopes:
  semgrep:
    target: .
    exclude:
      - src/shipgate/frontend/templates/
