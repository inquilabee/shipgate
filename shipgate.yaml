suite: full
env: managed
target: .
changed-only: true
error-format: compact
configs:
  mode: auto
checks:
  semgrep.scan:
    scope: semgrep
  radon.cc:
    threshold: B
  radon.mi:
    threshold: B
  jscpd.check:
    threshold: "4.5"
scopes:
  semgrep:
    target: .
    exclude:
      - src/shipgate/frontend/templates/
